#include<stdio.h>
int factorial(int);
int main()
{
    int n,a=0;
    printf("Enter your number :");
    scanf("%d",&n);
    a=factorial(n);
    printf("Factorial of %d is %d",n,a);
    return 0;
}
int factorial(int n)
{
    if(n==0)
    return 1;
    else 
    return n*factorial((n-1));
}
